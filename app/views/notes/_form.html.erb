<%= form_for [:user,@note], :html => {:class => "noteForm"}, remote:true do |f| %>
	<%= f.label :header %>
	<%= f.text_field :header %>

	<%= f.label :content %>
	<%= f.text_field :content %>

	<%= f.label :categories %>
	<%= f.text_field :categories, :value => "" %>

	<%= f.submit %>
<% end %>